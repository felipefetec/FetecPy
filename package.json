{
  "name": "fetecpy",
  "version": "0.1.0",
  "description": "Plataforma web de ensino de Python para iniciantes",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage"
  },
  "devDependencies": {
    "vitest": "^3.1.0"
  }
}
