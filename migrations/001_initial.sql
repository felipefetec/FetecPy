-- Migration 001 — schema inicial do FetecPy
-- Gerado pelo Prompt 1.2 — tabelas serão preenchidas naquele prompt
-- Este arquivo serve como placeholder da estrutura de migrations

SELECT 1; -- migration vazia intencional (Prompt 1.2 vai sobrescrever com o schema real)
